const express = require('express');
const router = express.Router();

// Placeholder — built in Phase 2
router.post('/register', (req, res) => res.json({ message: 'Auth module — coming in Phase 2' }));
router.post('/login', (req, res) => res.json({ message: 'Auth module — coming in Phase 2' }));

module.exports = router;
