const express = require('express');
const router = express.Router();

// Placeholder — built in Phase 2
router.get('/', (req, res) => res.json({ message: 'Products module — coming in Phase 2' }));

module.exports = router;
