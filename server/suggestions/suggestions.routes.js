const express = require('express');
const router = express.Router();

// Placeholder — built in Phase 5
router.get('/', (req, res) => res.json({ message: 'Suggestions module — coming in Phase 5' }));

module.exports = router;
