const express = require('express');
const router = express.Router();

// Placeholder — built in Phase 4
router.get('/', (req, res) => res.json({ message: 'List module — coming in Phase 4' }));

module.exports = router;
