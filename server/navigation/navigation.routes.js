const express = require('express');
const router = express.Router();

// Placeholder — built in Phase 6
router.get('/', (req, res) => res.json({ message: 'Navigation module — coming in Phase 6' }));

module.exports = router;
